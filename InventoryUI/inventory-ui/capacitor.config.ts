import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shrivo.app',
  appName: 'shrivo',
  webDir: 'dist/inventory-ui/browser'
};

export default config;
